# operator-test-generation-skill

This repository contains the `operator-problem-generator` Codex skill for generating operator problem assets for a contest or practice website.

Current scope:

- draft problem JSON contract
- NumPy reference implementation template
- structural JSON validator
- reference smoke-test runner
- directory-level validation for batches
- five MVP examples: `add`, `abs`, `maximum`, `sum`, `equal`
